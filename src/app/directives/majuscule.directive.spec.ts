import { MajusculeDirective } from './majuscule.directive';

describe('MajusculeDirective', () => {
  it('should create an instance', () => {
    const directive = new MajusculeDirective();
    expect(directive).toBeTruthy();
  });
});
