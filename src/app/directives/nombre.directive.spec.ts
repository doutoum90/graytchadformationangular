import { NombreDirective } from './nombre.directive';

describe('NombreDirective', () => {
  it('should create an instance', () => {
    const directive = new NombreDirective();
    expect(directive).toBeTruthy();
  });
});
